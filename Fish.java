package Animal;

public class Fish extends Animal {
    public Fish(String habitat, int averageAge, String name, int currentAge) {
        super(habitat, averageAge, name, currentAge);
    }
}
